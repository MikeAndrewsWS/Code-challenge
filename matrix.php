<?php
$matrix = [];

foreach ($matrix as $row)
    $i = 0;
    if ($row = 0){
        // if next row below is 0, move to that value in that array, and return to top of loop, +1 to i
        // if that value is 1, move to next value in THIS array

        // when end of final array is reached, return $i count
    }

?>